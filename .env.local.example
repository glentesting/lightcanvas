# Supabase — Postgres (projects table, JSONB) + Storage (audio, house photos)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# AI sequencer — generation fails loudly without this
ANTHROPIC_API_KEY=your-anthropic-api-key
# Optional dev flag: explicit deterministic mock planner, labeled in the UI
# AI_USE_MOCK=1
